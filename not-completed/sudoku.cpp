#include <iostream>
#include <string>
#include <random>
using std::cout, std::cin, std::string;

class wins{
private:
  

}

const uint8_t size; // how much tiles + squares inn field
uint16_t i; // x of array
uint16_t j; // y of array

int map(uint8_t array-map[i][j]) { // build the fiend with algorithm!
  i = j = size * size;  
  return field;
}

int main() {
  
  std::cout << "\nHeyo! Miyo the Crazey Tize return!!!";
  return 0;
}
